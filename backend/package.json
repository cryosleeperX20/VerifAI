{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@google/genai": "^1.27.0",
    "@tavily/core": "^0.5.12",
    "hono": "^4.10.3"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}